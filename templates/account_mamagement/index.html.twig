{% extends 'base.html.twig' %}

{% block body %}

    

    <div id="main">
                <div className="page-heading pb-2 mt-4 mb-2 ">
                    <h3>Manager Account</h3> <a href="insert.php"><button type="button" class="btn btn-outline-primary">Insert</button></a>
                </div>
                <div class="page-content">
                <div class="btn-group" role="group" aria-label="Basic outlined example">
               
                </div>
                <div class="container mb-3">
                    <table class="table table-striped">
                        <thead>
                            <tr>
                            <th scope="col">ID</th>
                            <th scope="col">Username</th>
                            <th scope="col">Password</th>
                            <th scope="col">CustomerName</th>
                            <th scope="col">Gender</th>
                            <th scope="col">BirthDay</th>
                            <th scope="col">Telephone</th>
                            <th scope="col">Address</th>
                            <th scope="col">Email</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for c in account %}
                                <tr>
                                    <td>{{c.id}}</td>
                                    <td>{{c.Username}}</td>
                                    <td>{{c.Password}}</td>
                                    <td>{{c.CustomerName}}</td>
                                    <td>{{c.Gender}}</td>
                                    <td>{{c.Birthday}}</td>
                                    <td>{{c.Telephone}}</td>          
                                    <td>{{c.Address}}</td>          
                                    <td>{{c.Email}}</td>          
                                    <td>
                                        <a href="update.php?id=<?=$row['Product_ID']?>" class="btn rounded-pill text-light" style="background-color: rgba(26,148,255,255);"> Update </a>
                                    </td>
                                </tr>     
                                {% endfor %}                        
                        </tbody>
                    </table>
             </div>
            </div>
    </div>
{% endblock %}
